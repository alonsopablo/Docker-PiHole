### Compose Files ###
Basic pihole: basic-pihole.yml
Custom IP pihole: mcvlan-pihole.yml

# Create the docker-compose file and paste the one you would like to use.
sudo nano docker-compose.yml

# Run the compose file
sudo docker-compose up -d

# Access to the container and change the password
sudo docker exec -it pihole bash
pihole -a -p
